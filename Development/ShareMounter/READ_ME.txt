mountShareAuto

Overview

* Automatically determines user share path via AD details loaded locally at imaging
* Automatically creates a folder on the dock and creates a shortcut to the share with custom icons

mountShare

Overview

* Allows user to specify user account, server, and share path to automatically create a mapped shortcut in their dock with custom icons






